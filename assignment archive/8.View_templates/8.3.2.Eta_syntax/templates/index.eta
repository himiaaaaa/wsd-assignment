<!DOCTYPE html>
<html>
  <head>
    <title>Hello there!</title>
  </head>
  <body>
    <h1>Value for <%= it.object %>: <%= it.value %> <% if(it.value > 0) { %>
        (positive)<% } else if (it.value < 0 ){ %>
        (negative)<% } else { %>
        (zero) <% } %>
    </h1>
  </body>
</html>